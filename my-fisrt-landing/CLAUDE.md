# CLAUDE.md — Russia Welcome Landing Page

## Что это за проект

Лендинг DMC-компании по приёму **арабских туристов в России** (B2B).
Цель: получить заявку от менеджера/владельца арабского турагентства через WhatsApp или форму.

**Аудитория:** турагентства из ОАЭ, Катара, Иордании, Саудовской Аравии, Кувейта.
**Устройства:** iPhone / мобайл — 85%+.
**Языки:** русский (по умолчанию), English, عربي (RTL).

---

## Стек

- Чистый HTML + CSS + JS (без фреймворков, без сборщика)
- Один файл: `index.html` (~2800 строк)
- Google Fonts: Cormorant Garamond (serif display), Inter (sans), Almarai (Arabic)
- Анимации: CSS transitions + IntersectionObserver (без GSAP)
- Иконки: emoji как placeholder

---

## Структура `index.html`

Секции сверху вниз:
1. NAV — sticky, lang toggle RU/EN/AR, WhatsApp кнопка
2. HERO — full-screen, частицы, fade-in анимации
3. TRUST BAR — 4 счётчика + логотипы стран
4. PAIN — 4 карточки болей агентств
5. PROCESS — 4 шага партнёрства
6. DESTINATIONS — Москва / Петербург / Сочи + Казань
7. SERVICES — 6 карточек услуг
8. HALAL — гарантии + 3 счётчика
9. TESTIMONIALS — 3 отзыва (placeholder)
10. ABOUT — текст + карточка менеджера
11. FAQ — аккордеон, 8 вопросов
12. CTA FORM — форма + контакты
13. FOOTER
14. WhatsApp float button (пульсирует)

---

## Дизайн-токены (CSS переменные)

```
--bg-light:   #FAF7F0   (Warm Ivory)
--bg-dark:    #0A1628   (Midnight Navy)
--bg-alt:     #F5F0E8   (Linen)
--brand:      #6B1F2A   (Deep Burgundy)
--gold:       #C9A84C   (Khaleeji Gold) ← главный акцент, все CTA
--gold-light: #E8C87A   (Champagne)
--teal:       #1A6B6B   (Arabian Teal)
--font-serif: Cormorant Garamond
--font-sans:  Inter
--font-arabic: Almarai
```

**Принципы:** luxury = много whitespace, медленные анимации (300–800ms ease-out), без bounce/elastic.

---

## Ключевые решения в коде

- **Анимации:** классы `.reveal`, `.reveal-left`, `.reveal-right` + `.stagger-1..6` — управляются IntersectionObserver в JS
- **Счётчики:** `data-counter="N"` на `<span>` — анимируются при входе в viewport
- **FAQ:** `toggleFaq(btn)` — аккордеон, одновременно открыт только один элемент
- **i18n:** `data-i18n="key"` на элементах, `setLang(lang)` в JS, объект `translations`
- **RTL:** `[dir="rtl"]` CSS правила + JS меняет `document.documentElement.dir`
- **WA float:** `id="waFloat"`, JS двигает его влево при AR
- **Dest cards:** CSS-градиенты как placeholder (без реальных фото)

---

## Placeholder данные (нужно заменить перед запуском)

| Что | Placeholder | Заменить на |
|-----|-------------|-------------|
| WhatsApp | `+7 900 123-45-67` | реальный номер бизнеса |
| Email | `info@russiawelcome.ru` | реальный email |
| Адрес | `ул. Тверская, д. 1, офис 302` | реальный адрес |
| OG image | `https://russiawelcome.ru/og-image.jpg` | реальный URL обложки 1200×630 |
| URL сайта | `https://russiawelcome.ru` | реальный домен |
| Имя менеджера | `Анастасия Волкова` | реальное имя |
| Testimonials | placeholder имена | реальные отзывы |
| Фото | emoji `👤` | реальные фото команды |
| Dest photos | CSS-градиенты | реальные фото городов (WebP) |

---

## Переводы (`translations` объект в JS)

Ключи с `data-i18n`:
- `nav.dest`, `nav.services`, `nav.halal`, `nav.about`
- `hero.label`, `hero.title`, `hero.sub`
- `hero.cta.wa`, `hero.cta.micro`, `hero.cta.catalog`
- `trust.halal`, `trust.guides`, `trust.countries`, `trust.reply`
- `wa.label`, `wa.micro`

**Hero title** рендерится через `innerHTML` (сохраняет `<em>`), все остальные через `textContent`.

---

## Файлы проекта

```
my-fisrt-landing/
├── index.html       ← единственный рабочий файл
├── brief.md         ← полное ТЗ (контент, дизайн, стек)
├── research.md      ← исследование рынка (7 агентов)
└── CLAUDE.md        ← этот файл
```

---

## Что сделано

- [x] Все 13 секций по брифу
- [x] Responsive (1024 / 768 / 480px breakpoints)
- [x] RTL поддержка для арабского
- [x] Анимации scroll-reveal + stagger
- [x] Animated counters
- [x] FAQ accordion
- [x] WhatsApp float + pulse
- [x] Lang toggle RU/EN/AR
- [x] OG / Twitter meta tags
- [x] Schema.org JSON-LD (TouristInformationCenter)
- [x] Favicon (inline SVG)
- [x] `prefers-reduced-motion` support

## Что осталось (контент)

- [ ] Реальные фото направлений (WebP + `<picture>`)
- [ ] Реальный WhatsApp номер
- [ ] Реальные testimonials от агентств
- [ ] PDF каталог программ
- [ ] Яндекс.Метрика + GA4 (вставить счётчики)
- [ ] Полный Arabic перевод всех секций
